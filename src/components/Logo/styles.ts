import styled from "styled-components";
import { fontSizes, verticalSpacing } from "../../constants/theme";

export const CustomLogo = styled.h1`
  align-self: flex-end;
  font-size: ${fontSizes.xxl};
  margin-bottom: ${verticalSpacing.md2};
`;